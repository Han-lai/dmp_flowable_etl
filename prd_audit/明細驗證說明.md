# 指標明細驗證說明

> 本文件說明：KPI 報表的數字是怎麼來的、明細檔包含哪些資料、哪些被排除了，以及如何用明細檔反查驗證報表數字。

---

## 一、明細檔與 KPI 報表的對應關係

KPI 報表（金層）的每一個數字，都可以在本次匯出的 Excel 明細檔中找到對應的任務記錄。

**驗證邏輯**：
- 報表的「**Total**」= 明細中同一線別、同一日期的**所有資料列數**
- 報表的「**Done**」= 明細中 `status_daily` / `status_weekly` / `status_monthly` 欄位值為 `DONE` 的列數
- 報表的「**Todo**」= 值為 `TODO` 的列數
- 報表的「**Doing**」= 值為 `DOING` 的列數

> 篩選時請根據報表的時間粒度選擇對應的欄位，說明見下方第三節。

---

## 二、明細檔說明

### 檔案清單

| 檔案名稱 | 涵蓋資料 | 筆數 |
|----------|---------|------|
| V1_CNE.xlsx | V1 流程 / CNE 區域 | 600,441 |
| V1_CNS.xlsx | V1 流程 / CNS 區域 | 942,378 |
| V1_CNW.xlsx | V1 流程 / CNW 區域 | 15,056 |
| V2_CNE.xlsx | V2 流程 / CNE 區域 | 62,210 |
| V2_CNS.xlsx | V2 流程 / CNS 區域 | 43,485 |
| V2_CNW.xlsx | V2 流程 / CNW 區域 | 486 |
| V3_CNE.xlsx | V3 流程 / CNE 區域 | 1,584,333（分月份 Sheet） |
| V3_CNS.xlsx | V3 流程 / CNS 區域 | 944,648 |
| V3_CNW.xlsx | V3 流程 / CNW 區域 | 10,501 |

**資料日期範圍**：2025-10-01 ～ 2026-06-15

### 重要說明

明細檔已預先過濾，**只包含正式生產任務**（`is_excluded = 0`），與 KPI 報表計算口徑完全一致。被排除的系統任務、測試單等**不會出現在明細檔中**，說明見第四節。

---

## 三、日 / 週 / 月 的結算狀態欄位不同

這是最常造成驗證數字對不上的原因。

明細中有三個專用欄位，分別對應不同的報表粒度：

| 報表粒度 | 應看的欄位 | 結算截止點 |
|----------|-----------|-----------|
| **日報** | `status_daily` | 開單**當天** 23:59:59 |
| **週報** | `status_weekly` | 開單當週**週日** 23:59:59 |
| **月報** | `status_monthly` | 開單當月**最後一天** 23:59:59 |

### 同一筆任務，三個欄位的值可能不同

舉例：某任務 1/28（週二）開單，2/3（隔週一）才完工

| 欄位 | 值 | 原因 |
|------|-----|------|
| `status_daily` | `TODO` | 1/28 當天沒有任何動作 |
| `status_weekly` | `TODO` | 在 2/2（週日）之前未完工 |
| `status_monthly` | `DONE` | 在 1/31 月底前完工 ✓ |

### 什麼是「開單日」？

KPI 的時間基準是**開單日（`task_start_date`）**，不是完工日。  
一張 12/30 開出的工單，即使 1/5 才完工，它仍然算在 **12 月**的報表裡。

---

## 四、哪些任務被排除、不計入 KPI

以下類型的任務不納入 KPI 計算，也**不會出現在本次匯出的明細檔中**：

| 排除原因 | 說明 | 如何識別 |
|---------|------|---------|
| **自動跳過節點** | 流程中系統自動完成、無需人工介入的步驟 | 任務被標記 `autoComplete = 1` |
| **系統帳號操作** | 處理人為 SYSTEM，非真實人員操作 | `assignee_name = 'SYSTEM'` |
| **系統流程節點** | 流程引擎內部的自動化路由節點 | 任務代碼以 `E` 或 `C` 開頭 |
| **Q 測試工單** | 品質驗證用的測試單，非正式生產 | 工單號以 `Q` 開頭 |
| **R 測試工單** | 維修驗證用的測試單，非正式生產 | 工單號以 `R` 開頭 |
| **通知任務** | 純通知性質，無需人工處理 | 任務名稱含 `Notify` |
| **虛擬任務** | 流程設計用的佔位任務 | 任務名稱含 `Dummy` |

---

## 五、V1 / V2 / V3 的分類規則

明細檔依 `vx_type` 欄位分檔，分類規則如下：

### V1 — 標準生產流程

包含以下情況，**任一符合即歸 V1**（優先順序由上到下）：

1. **NPE 廠區的非出貨任務**：工廠為 NPE，且任務代碼**不是** V2 開頭 → 強制歸 V1
2. **特殊工單前綴**：工單號前三碼為 `196`、`199`、`200`、`210`、`212`、`213` → 歸 V1
3. **任務代碼 V1 開頭**：標準 V1 流程

> **為什麼 NPE 廠區要特別處理？**  
> NPE 廠區的流程雖然在系統中以 V3 代碼建立，但業務性質與 V1 生產流程相同，因此強制歸 V1 統計，避免 V3 數字虛高。

### V2 — 出貨行政流程

- 任務代碼以 `V2` 開頭
- NPE 廠區的 V2 出貨任務維持 V2（不受上述 NPE 規則影響）

### V3 — 品質異常流程

- 任務代碼以 `V3` 開頭（且不屬於 NPE 廠區的非出貨任務）

---

## 六、驗證步驟建議

以「V3 / CNE / 2026-06 月報 Done 數量」為例：

1. 開啟 `V3_CNE.xlsx`，切換到 `2026-06` Sheet
2. 在 `status_monthly` 欄位篩選值為 `DONE`
3. 再依需要加篩：`region = CNE`、`line = 目標線別`
4. 計算篩選後的筆數，應與月報該線別的 Done 數字一致

若數字有差異，請先確認：
- 月報的時間範圍是否涵蓋整個月（部分月份明細可能跨兩個 Sheet）
- 篩選的是 `status_monthly` 而非 `status_daily` 或 `task_status`
- 明細日期範圍：任務的 `task_start_date` 是否落在該月（KPI 以開單日為準）

---

## 七、欄位速查

| 欄位名稱 | 說明 |
|---------|------|
| `task_id` | 任務唯一識別碼 |
| `proc_inst_id` | 流程實例 ID（同一工單可能包含多個任務） |
| `task_name` | 任務名稱（如：組裝、測試、包裝） |
| `task_start_date` | **開單日（KPI 時間基準）** |
| `task_end_date` | 完工日（空白 = 尚未完工） |
| `status_daily` | 日報結算狀態：TODO / DOING / DONE |
| `status_weekly` | 週報結算狀態：TODO / DOING / DONE |
| `status_monthly` | 月報結算狀態：TODO / DOING / DONE |
| `task_status` | 即時狀態（查詢當下），不用於報表驗證 |
| `vx_type` | 業務分類：V1 / V2 / V3 |
| `region` | 區域：CNE / CNS / CNW |
| `plant` | 廠別 |
| `factory` | 工廠 |
| `line` | 線別 |
| `mo_number` | 工單號 |
| `model_name` | 機種 |
| `assignee_name` | 處理人姓名 |
| `duration_min` | 開單到完工的總時長（分鐘），未完工為空白 |
| `processing_time_min` | 簽收到完工的實際處理時長（分鐘） |
