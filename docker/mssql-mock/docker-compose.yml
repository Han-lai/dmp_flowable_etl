services:
  mssql:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: mssql-mock
    ports:
      - "1433:1433"
    environment:
      - ACCEPT_EULA=Y
      - MSSQL_SA_PASSWORD=YourStrong@Passw0rd
    volumes:
      - mssql-data:/var/opt/mssql
      - ./init:/docker-entrypoint-initdb.d
    networks:
      - mssql-net

volumes:
  mssql-data:

networks:
  mssql-net:
    driver: bridge
